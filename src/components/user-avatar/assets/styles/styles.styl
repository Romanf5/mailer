.avatar-wrp {
  position: relative;
}

.user-menu {
  position: absolute;
  left: 0;
  top: 100%;
  background-color: #fff;
  width 80px;
  padding: 15px
  border-radius: 5px;
  box-shadow: 0 3px 6px 0 rgba(0, 0, 0, 0.5);
  z-index: 20;
  text-align: center;
  cursor: pointer;
}

.test {
  width 40px;
  height 40px;
}
